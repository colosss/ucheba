#include <iostream>

using namespace std;

class Client {
private:
    string name;
    string passport;
    string address;
    string number;

public:
    Client(string name, string passport, string address, string number) {}
    ~Client() {}
    
};