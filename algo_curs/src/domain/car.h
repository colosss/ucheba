#include <iostream>

using namespace std;

class Car{
private:
    string number;
    string model;
    string color;
    int year;
    bool isAvilable;
public:
    Car(string number, string model, string color, int year, bool isAvilable){}
    ~Car(){};
};