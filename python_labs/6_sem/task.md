Твоя задача сделать задания по python, главаная особенность для каждого задания создана отдельная папка 6_sem/("название папки"), пример, первое задание нужно выполнить в папке 6_sem/lr1
(Для окружения исползуй uv, туда устанавливай все нужные библиотеки для выполнения заданий)


lr1: Задание должно содержать минимум 3 теста и быть реализовано с использованием Type Hinting. Напишите класс машина, у которой имеются следующие состояния: стоп, поворот налево, поворот направо, движение вперед, разгон. Каждый раз, когда у неё меняется состояние, должна осуществляться запись в текстовый логирующий файл. Формат записи в лог-файл: 1) дата и время изменения состояния; 2) предыдущее состояние; 3) текущее состояние.

lr2: Скачайте с сайта с сайта ЕМИСС (ГОСУДАРСТВЕННАЯ СТАТИСТИКА) данные по средним потребительским ценам на товары, реализуемых на розничных рынках Российской Федерации с 2007 по 2015 год (https://www.fedstat.ru/indicator/37055). Выведите динамику цен по продукту «Масло сливочное» и «Окорочка куриные». Найдите среднее значение цены, медиану за весь период, а также посчитайте отклонения месячных цен от среднего и медианы.  Для наглядности используйте графики. (Ничего скачивать ненужно, я сам скачал датасет и поместил его в папку lr2)

lr3: Задание должно сопровождаться минимум 5 запросами и содержать минимум 2 теста тестами под каждый запрос, а так же быть реализовано с использованием Type Hinting. Минимальное количество таблиц в базе данных – 4.
Придумайте и реализуйте базу данных, взяв за основу предметную область: Клиническая больница.

lr4: Познакомиться с основными способами работы с SQLite средствами ORM SQLAlchemy. Задание должно сопровождаться минимум 5 запросами и содержать минимум 2 теста тестами под каждый запрос, а так же быть реализовано с использованием Type Hinting.
Задание должно выполнять средствами ORM SQLAlchemy. Придумайте и реализуйте базу данных, взяв за основу предметную область: Игровой магазин.

lr5: Используя потоки, напишите программу, реализующую задачу про обедающих философов: «За круглым столом расставлены пять стульев, на каждом из которых сидит философ. В центре стола размещено блюдо с макаронами. На столе лежат пять вилок, каждая из которых находится между двумя соседними тарелками. Каждый философ может находиться в двух состояниях: размышлять или есть макароны. Для того, чтобы начать есть, философу необходимы две вилки: одна в правой руке, а другая в левой. Закончив еду, философ кладет вилки на место и начинает размышлять до тех пор, пока снова не проголодается.» 

lr6: Выполненное задание должно содержать минимум 3 теста и быть реализовано с использованием Type Hinting. Реализуйте асинхронный генератор, который возвращает случайные числа в течение определенного времени.

lr7: Выполненное задание должно содержать минимум 3 теста и быть реализовано с использованием Type Hinting.
В базе данных должно быть минимум 5 таблиц и минимум 10 запросов.
telegram-бот для управления финансами


Выводы:

lr1: В ходе работы был реализован класс машины с фиксированным набором состояний: остановка, движение вперед, разгон, поворот налево и поворот направо. Для описания состояний был применен Enum, что позволило сделать код более надежным и избежать передачи произвольных строк. При изменении состояния реализована запись перехода в текстовый лог-файл с датой, предыдущим и текущим состоянием. Также были применены Type Hinting, dataclass и модуль pathlib для удобной работы с путями. Для проверки корректности работы написаны тесты, которые контролируют начальное состояние, формат логирования и отсутствие лишних записей при повторной установке того же состояния.

lr2: В работе была реализована обработка Excel-файла с данными о средних потребительских ценах. Для загрузки и анализа данных применялась библиотека pandas, а для построения графика динамики цен - matplotlib. Программа выделяет нужные товары, формирует временной ряд цен, рассчитывает среднее значение, медиану, а также отклонения месячных цен от этих показателей. Такой подход позволяет не только получить итоговые статистические характеристики, но и увидеть изменение цен во времени. Дополнительно предусмотрена обработка ситуации, когда в исходном файле отсутствуют нужные товары, чтобы программа завершалась корректно и выводила понятное сообщение.

lr3: В ходе работы была спроектирована и реализована SQLite-база данных для предметной области "Клиническая больница". В схему вошли таблицы отделений, врачей, пациентов, приемов и назначенного лечения, связанные между собой внешними ключами. Для работы с базой применялись SQL-запросы через стандартный модуль sqlite3, что позволило закрепить навыки создания таблиц, заполнения данных и выборки связанных записей. Были реализованы запросы для получения врачей отделения, приемов пациента, нагрузки врачей, количества пациентов по отделениям и активных назначений. Для каждого запроса подготовлены тесты, проверяющие как основной сценарий, так и граничные случаи.

lr4: В данной лабораторной работе была реализована база данных игрового магазина средствами ORM SQLAlchemy. Вместо ручного написания всех SQL-запросов использовались ORM-модели, связи relationship, типизированные поля Mapped и выражения select. В базе представлены категории игр, издатели, игры, покупатели, заказы и позиции заказов. Реализованные запросы позволяют получать игры по категории, определять лидеров продаж, считать траты клиентов, находить товары с малым остатком и выводить заказы с позициями. Работа показала, как ORM упрощает взаимодействие с реляционной базой, сохраняя при этом возможность строить сложные запросы и агрегировать данные.

lr5: В ходе работы была реализована многопоточная модель задачи об обедающих философах. Для философов использовались отдельные потоки, а вилки были представлены объектами threading.Lock, что позволило смоделировать конкуренцию за общие ресурсы. Чтобы избежать взаимной блокировки, был применен упорядоченный захват вилок: каждый философ берет сначала вилку с меньшим номером, затем со старшим. Программа фиксирует состояния философов и количество приемов пищи, а после завершения симуляции все потоки корректно завершаются. Тесты проверяют успешное выполнение сценария, ограничение на число философов и итоговое состояние после обеда.

lr6: В работе был реализован асинхронный генератор случайных чисел, который выдает значения в течение заданного времени. Для организации асинхронного выполнения применялись async, await, asyncio.sleep и асинхронная итерация async for. Генератор поддерживает настройку длительности работы, интервала между выдачей чисел и диапазона случайных значений. Дополнительно реализована вспомогательная функция для сбора результатов в список, что удобно для тестирования. Проверки подтверждают корректность диапазона чисел, поведение при нулевой длительности и обработку ошибочных параметров.

lr7: В ходе работы была реализована основа Telegram-бота для управления личными финансами с хранением данных в SQLite. База данных включает пользователей, счета, категории, операции, бюджеты и финансовые цели, что покрывает основные сущности финансового учета. В сервисном слое реализованы операции добавления доходов и расходов, расчет баланса, месячная сводка, расходы по категориям, контроль бюджета, список целей и последние операции. Для обработки команд бота создан отдельный обработчик, который интерпретирует текстовые команды и возвращает готовый ответ пользователю. Такой подход разделяет хранение данных, бизнес-логику и интерфейс Telegram-бота, а тесты проверяют ключевые финансовые расчеты и обработку команд.
